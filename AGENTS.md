<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

## Standing video rules
Read docs/production-rules.md before video work. Every video and production segment must contain an established DLL Studio character. Deliver a continuous standard episode; join internal three-minute production segments into one long-form master. Leo is the existing-character lead for Captain Giggle.


Current episode target: 10 minutes (three 3-minute segments plus a 1-minute ending). Videos require actual character-performance animation matching videos.zip; static portraits with CSS motion do not satisfy delivery. See docs/production-rules.md.

Website freeze: preserve the Captain Giggle website as-is. The 10-minute animated video is independent; use docs/independent-video and scripts/independent-video. Do not rewrite the website timeline as part of video production. On September 25 the user explicitly authorized integrating Story Lab into the main site and publishing to GitHub and Vercel production; that deployment is permitted while preserving the companion behavior.

